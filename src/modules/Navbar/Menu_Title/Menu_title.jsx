import React from 'react'

function Menu_title({ title }) {
    return (
        <div class="menu-title">
            <p>
                {title}
            </p>
        </div>
    )
}

export default Menu_title